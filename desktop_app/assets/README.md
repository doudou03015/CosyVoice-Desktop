# 应用图标

`app.png` 是为本桌面项目生成的独立图标原稿，采用深蓝底、青色对话气泡与声波。
它不是 CosyVoice 上游官方标志，随本项目使用仓库 LICENSE 分发。

`app.ico` 含 16、24、32、48、64、128、256 像素七档透明图像，
供 Windows EXE、安装器、快捷方式、任务栏和窗口标题栏使用。
界面侧栏使用同一原稿。运行 `python packaging/build_icon.py` 可重新生成 ICO。
